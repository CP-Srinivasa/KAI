# KAI CODE-MAP — Anker-Index (Verify-Pointer, Stand 2026-06-22)

Zweck: die meist-gesuchten Code-Pfade an EINEM Ort, damit Agenten/Helfer den Worktree nicht jedes Mal neu durchsuchen müssen.

**Regeln:**
1. Einträge sind **Verify-Pointer** — vor dem Zitieren als Fakt gegen den aktuellen Code prüfen (Code bewegt sich).
2. Wer einen gemappten Pfad ändert/verschiebt, **aktualisiert diese Datei im SELBEN PR** (kein separater Pflegeaufwand, kein Verrotten).
3. Nur **Hochfrequenz-Anker** — bewusst NICHT die ganze Codebase.
4. **Keine Live-Werte hier** (die driften) — Flag-LIVE-Werte stehen im Pi-`.env`; aktueller Betriebs-Stand im Memory-Block „AKTUELLER STAND".

## Code-Anker

### Orchestrierung / Loop
- `app/orchestrator/trading_loop.py` → `TradingLoop`, `run_trading_loop_once` — 7-Step-Pipeline + Cycle-Audit

### Entry / Modi / Gates
- `app/core/enums.py` → `EntryMode` (disabled/paper/paper_premium_limited/paper_learning/probe/live_limited/live_normal); `.allows_autonomous_loop_entry` = True NUR für paper/probe/live_*
- `app/execution/entry_policy.py` → `EntryRoute`, `detect_contradictions`, Route-Verdicts (autonomous_loop/premium_paper/real_analysis_paper/fastlane/technical)
- `app/risk/engine.py` → `RiskEngine` (Sizing/Drawdown/Veto) · `app/risk/promotion_gate.py` → Bleed-Breaker
- `app/security/governance/gates.py` → `authorize_productive_decision` — **kein Kettenglied**: `trading_loop.py` importiert `app.security.governance` nicht, der einzige Produktionsaufrufer (`app/orchestrator/governed_decision.py`) wurde am 2026-09-04 entfernt. Geprüftes Primitiv ohne Aufrufer.

### Deploy / systemd
- `deploy/bin/kai-service-control` → Service-Broker (einziger passwortfreier Root-Pfad für `ubuntu`-Units: start/stop/restart/reload/status, Invariante: User=ubuntu)
- `scripts/standby_to_usb.sh` → Cold-Standby-Sicherung nach `/mnt/kai-data/kai-standby/` (System/Deps/Data/Artifacts + Standby-Manifest via `write_manifest()`)
- `scripts/lib/pi_unit_sync.sh` → `pi_unit_sync_diff` / `pi_unit_sync_apply`: gleicht `deploy/systemd/*` gegen `/etc/systemd/system` ab. Der Deploy ruft seit 2026-08-20 nur noch `pi_unit_sync_diff` (read-only) auf; `pi_unit_sync_apply` braucht Privilegien, die der Broker bewusst NICHT vergibt, und gehoert dem Operator. **committet != live** galt bis 2026-08-18 fuer jede Unit-Datei. Timer werden neu gestartet (sonst uebernimmt systemd den Zeitplan nicht), `.service` NIE (SIGTERM-Fehlalarm 17.08.), eingefrorene Writer gar nicht angefasst, `/etc`-Waisen nur gemeldet. Enable/Start bleibt bei `scripts/pi_install_systemd.sh`. Release-gebundene `.service` (`--repo`/WorkingDirectory auf `…/current`) werden NUR in ein aktives Release kopiert, sonst laut `SKIPPED_RELEASE_NOT_ACTIVE <unit>` + rc=10 (Vorfall 2026-09-04: fuenf Units nach /etc, `current` fehlte, Restart 200/CHDIR).
- `scripts/lib/pi_release_guard.sh` → `pi_release_unit_target` / `pi_release_active_reason` / `pi_release_bound_units` / `pi_release_hint`: EIN Kriterium fuer Release-Bindung (runtime-exec `--repo` oder WorkingDirectory auf `…/current`) und „Release aktiv" (Verzeichnis + `release.json` + `.venv/bin/python`), geteilt von `pi_install_systemd.sh` (`assert_release_ready`, #855) und `pi_unit_sync.sh`. Prueft KEINEN Baum-Hash (das ist `verify_release`). Runbook: `docs/deploy/immutable_release_cutover.md` § Apply vor Cutover.
- `scripts/pi_deploy_step.sh` → DER Deploy-Schritt, laeuft auf der Pi. `kai_deploy.sh` in `~/KAI-mirror` ist nur noch ein ssh-Shim. Grund (2026-08-20): der Wrapper lag ausserhalb Versionskontrolle und CI, und `pi_unit_sync_apply || echo` verwandelte dort einen an `sudo: a password is required` gescheiterten Sync in ein gruenes Deploy — 24 Unit-Dateien blieben divergent. Der Schritt **misst** den Unit-Drift nur (read-only); anwenden ist operator-privilegiert.
- `scripts/lib/pi_deploy_verdict.sh` → `pi_deploy_reasons` / `pi_deploy_verdict` / `pi_deploy_explain`: Fakten → Tokens → kanonischer Exit-Code (**0 SUCCESS · 10 HOLD · sonst FAILED**). Strengster Grund gewinnt; unbekanntes Token faellt auf FAILED. `/health=200` ist Nachbedingung, **nie** Urteil.
- `scripts/pi_apply_systemd_units.sh` → der OPERATOR-Weg (Passwort, auf der Pi), den das `DEPLOY_HOLD` verlangt: sichert nach `/var/backups/kai-units/<ts>/` **bevor** geschrieben wird, wendet via `pi_unit_sync_apply` an, **beweist** (Byte-Gleichheit je Datei · `active` je Timer · endlicher `NextElapse` je Timer) und rollt bei Fehlschlag zurueck. Eingefrorene Writer werden aus dem Beweis genommen — zurueckgestellt ist kein Fehlschlag (rc=10). Release-gebundene Units ohne aktives Release auf dem Host werden uebersprungen, beim Namen genannt, rc=10 auch im `--dry-run` — kein Rollback, keine Blockade der uebrigen Units (Vorfall 2026-09-04). Kein Transaktions-, sondern ein Rueckweg.
- `scripts/lib/paper_writer_freeze.sh` → `paper_writer_freeze_guard_restart`: fail-CLOSED Freeze-Semantik, von Installer, Deploy-Wrapper und Unit-Sync gemeinsam genutzt.

### Execution / Paper
- `app/execution/paper_engine.py` → `PaperExecutionEngine` (Fills/Close/MTM/Slippage) · `app/execution/models.py` → `PaperFill/PaperOrder/PaperPortfolio`
- `app/execution/audit_replay.py` → `replay_paper_audit`
- **Phantom-Closes:** kanonische Schwelle in `app/execution/phantom_filter.py` (`paper_engine` importiert sie — NIE ein zweites Mal definieren). Schreibpfad weist ab + `close_price_sanity_rejected`; Lesepfad `portfolio_read` → `bayes_quarantine` → `phantom_filter` verschiebt nach `quarantined_pnl_usd` (löscht nichts). Bestand + Regeln: `docs/audit/phantom_close_artifact_register.md`

### Edge / Shadow / Resolver
- `app/observability/shadow_candidate_ledger.py` → `build_shadow_report`, `_median`/`_split` (median-only, `fwd_*_bps`)
- `app/observability/shadow_resolver.py` → `resolve_with_binance` (Kline-Forward-Returns)
- `app/observability/edge_report.py` → Cohort-Edge + `_median`/`_winsorized_mean` (`WINSOR_LIMIT_BPS=500`, Median-GO-Gate)
- `app/observability/generator_edge_collector.py` → `collect_edge_inputs_from_resolved` (IC/Brier-Paare) · `generator_edge.py`
- **Edge-Discovery-Engine** (systematische Hypothesen-Suche auf eigenem OHLCV, NICHT live-Shadow): `app/research/{runner,evaluate,samples,stats,multiple_testing,ledger}.py` (Benjamini-Hochberg-FDR + Survival-Gates) ← `app/analysis/features/{feature_matrix,forward_returns}.py` (kausale Features + Forward-Label, No-Lookahead) ← `app/market_data/{history_loader,kline_windows}.py` (paginierter Backfill). Lauf: `python -m app.research.runner` → `artifacts/research/edge_search_*.json`; **Hypothesen-Ledger** (kumulativ, nie blind re-testen) → `artifacts/research/hypothesis_ledger.jsonl` (`ledger.hypothesis_key` = datenfenster-agnostische Config-ID). ⚠ Features kausal ≤i, Label vorwärts i+h — nie vermischen.
- **Zwei Label-Konventionen, bewusst nebeneinander** (`app/analysis/features/forward_returns.py`): `compute_forward_return_bps` = Close-zu-Close, die Konvention der **zwoelf versiegelten** TA-Verdikte — unangetastet. `compute_next_open_forward_return_bps` = ausfuehrbar: `Signal close(t) → Entry open(t+1) → Exit close(t+h)`. ⚠ Die Fehllesart `EXIT = t+1+h` liegt eine volle Kerze daneben; an Zeitstempeln gepinnt in `tests/unit/test_volume_z_and_reentry.py`.
- **Praeregistrierte Primaerhypothese** `rsi_reentry_volume_confirmed` (`app/research/runner.py`): `rsi_14_prev`-Crossover + `volume_z_20 >= 2.0`. **NICHT** in `default_hypotheses()` — die 12er-Familie ist eine versiegelte BH-FDR-Familie, ein 13. Mitglied hoebe deren Huerde rueckwirkend. `primary_confirmatory_hypothesis()` (m=1, gatend) vs. `secondary_benchmark_family()` (13, NON_GATING/NON_PROMOTIONAL). ⚠ `open` gehoert NIE in `FeatureRow` — Merkmal = Wissen zum Signalzeitpunkt, Label = danach Realisierbares. Spec: `docs/superpowers/specs/2026-08-19-tv-path-decide-design.md`.

### Signal / Evidence
- `app/signals/generator.py` → `SignalGenerator` (6 Filter) · `app/signals/models.py` → `SignalCandidate`
- `app/signals/bayesian_confidence.py` → `BayesianConfidenceEngine`; **`direction_aligned` = pro/contra-Signal, NICHT realisiertes Outcome**
- Evidence-Settings: `app/core/evidence_settings.py` (`HypeEvidenceSettings` u.a.) + Wiring `app/signals/*_wiring.py`

### Markets / Sources
- `app/market_data/{momentum,oi_zscore,sentiment,liquidations,coingecko_adapter,binance_adapter}.py` (`binance_adapter.get_ohlcv(start_time_ms=…)` = historischer Backfill-Anker; Payload-Validierung in `_parse_kline_rows`, fail-closed vs NaN/Inf)
- `app/ingestion/rss/adapter.py` → `RSSFeedAdapter` (published_at via `calendar.timegm`, NICHT `mktime` — TZ-Bug #362) · `app/ingestion/classifier.py` · API-Adapter `app/ingestion/messari/adapter.py` (CryptoPanic-Adapter 2026-09-02 entfernt: nie importiert)

### AI Control Plane (LLM-Aufrufschicht)
- `app/ai/` → **die Aufruf-Schicht** fuer JEDEN LLM-Call. Besitzt Observability + Ketten-Policy, NICHT Transport und NICHT Provider-Konstruktion. Keine eigene Dependency, kein eigener Prozess, kein eigener Artefakt-Strom.
  - `app/ai/audit.py` → `llm_call_scope()` (genau EINE Telemetrie-Zeile je Call, Erfolg wie Fehler, reraise immer) · `classify_error()` (geschlossene Taxonomie timeout/rate_limit/auth/quota/schema/refusal/transport/server/cancelled/unknown, duck-typed, wirft nie) · `is_retryable_error()` (Deny-List, speist die `tenacity`-Filter der vier Provider) · `correlation_scope()`/`current_correlation_id()` (ContextVar — die ID darf NICHT durchs Prompt-`context`-dict reisen, das wuerde den Prompt-Text aendern)
  - `app/ai/pricing.py` → **die einzige Preistabelle im Repo** (D-CORE-007). `PRICE_TABLE` (Modell → USD/1M in+out) + `PRICE_TABLE_VERSION` + `estimate_cost_usd()`. Exakter Namensvergleich nach Normalisierung (Anbieter-Praefix weg, Kleinschreibung) — `gpt-4o-2024-08-06` faellt bewusst auf `COST_UNKNOWN` statt auf `gpt-4o` gebogen zu werden. Was nicht in der Tabelle steht, hat KEINEN Preis: kein Rueckfallwert, kein Durchschnitt, **nie 0**. Rein: keine Uhr, kein I/O.
  - `app/ai/spend.py` → der Leser, der dem Budget bis 2026-09-08 fehlte. `spend_window("today"|"month")` ueber `artifacts/llm_telemetry.jsonl` (KEIN neuer Strom), Fenster auf **UTC-Mitternacht** bzw. **Monatsersten**, nicht rollend. Besitzt die kanonische Zaehlebene `dedupe_chain_levels()` und den Anbieterfilter `is_ai_row()` — `app/ai/health.py` importiert beides von hier, damit Gesundheit und Budget nicht zwei Meinungen ueber dieselbe Grundgesamtheit haben. mtime-Cache, fail-soft.
  - `app/ai/budget.py` → zusaetzlich zu `decide()`/`accumulate()` jetzt `evaluate_status()` (OK | WARNING | LIMIT_REACHED | COST_UNKNOWN), `BudgetStatus` und die typisierte `BudgetExceeded`. `BUDGET_EXEMPT_ROUTES = {critical}` ist **nicht konfigurierbar**: wer `intent` sperrt, nimmt dem Operator die Fernbedienung. `COST_UNKNOWN` ist der einzige fail-closed Zweig (zu viele unbelegte Aufrufe → Routine wie LIMIT_REACHED).
  - `app/core/ai_cost_settings.py` → `APP_AI_BUDGET_*`, `APP_ANALYSIS_SHADOW_ENABLED`. Eigene Datei, weil `app/core/settings.py` mit 1.883 Zeilen exakt auf seiner God-File-Ratchet-Baseline steht. Default = heutiges Verhalten (keine Limits, Schatten an).
  - `app/ai/health.py` → `ai_health_snapshot()` rein aus dem Telemetriestrom, **keine Probe-Calls**; `state` trennt `ok`, `disabled`, `not_configured`, `unavailable` und `error`; `status_reason=no_recent_calls` bei n=0 (nie `ok`). Quell-Telemetrie wird vor der Deduplizierung ausgeschlossen. Endpoint `GET /health/ai` (`app/api/routers/health.py`) — auth-gated wie `/health/timers`, NICHT in der Public-Liste von `app/security/auth.py:298`. `HealthResponse` bleibt unangetastet. Seit D-CORE-007 zusaetzlich der **additive** Schluessel `cost` (`cost_block()`): heute/Monat getrennt, `*_known` neben `unknown_cost_calls_*` (die Summe ist eine UNTERGRENZE), `status`, `price_table_version` und der Satz „estimates from list prices; billing amounts are separate“. Kein Probe-Call, kein neues Dashboard.
- `app/analysis/factory.py` → Provider-**Konstruktion**: `create_provider(type, settings)` · `create_primary_provider()` (openai → gemini → (grok), Alias `create_cli_primary_provider`) · `create_shadow_provider()` (anthropic → gemini) · `describe_primary_chain()`/`describe_shadow_chain()` liefern die Namen OHNE Clients zu bauen (Quelle fuer `/health/ai`). Seit NEO-P-003 nutzt auch `app/api/main.py` die Primaerkette; `PIPELINE_PROVIDER` wirkt nur noch als **expliziter** Override (`model_fields_set`).
- `artifacts/llm_telemetry.jsonl` → **v2** (`app/observability/llm_telemetry.py`): v1-Felder unveraendert an gleicher Stelle + `correlation_id, call_id, purpose, chain_position, attempt, error_class, http_status, prompt_tokens, completion_tokens, outcome`. `chain_position=-1` = aeussere Zeile ueber die ganze Kette, `>=0` = ein Versuch im `EnsembleProvider`. Wer zaehlt, muss deduplizieren (siehe `app/ai/health.py::_load_rows`), sonst zaehlt jeder Ensemble-Call doppelt. ⚠ **Zweite Falle, teurer als die erste:** der Strom traegt BEIDE Schema-Generationen. `v1` (bis 2026-09-02 19:33, 13.074 Zeilen) hat nur 8 Felder und KEINE Usage; `v2` (ab 20:11) fuehrt sie vollstaendig. Ein 30-Tage-Fenster mischt beide und sieht dann aus wie ein Erfassungsdefekt — vor jeder Auswertung erst `schema_version` und `chain_position` auszaehlen, nicht danach. Und `provider` traegt auch Quellnamen (CNBC, BeInCrypto, `rss_feed`): auf {openai, anthropic, gemini, grok} filtern, so wie es `app/ai/health.py::_is_ai_row` tut. `llm_telemetry_summary()` (Dashboard) liest v2 unveraendert. **Seit D-CORE-007** zusaetzlich `cost_usd`/`cost_known`/`cost_source`/`cost_status`/`cost_reason`, `total_tokens`, `use_case`, `escalation_reason` und `source`. `cost_source` trennt **Abrechnung** (`upstream`, vom Anbieter genannt) von **Schaetzung** (`list_price:<version>`, aus Token gerechnet); `null` + `COST_UNKNOWN` ist die dritte, ehrliche Moeglichkeit — **nie 0**. Der Quellname gehoert ab jetzt in `source`, nicht in `provider` (`app/analysis/pipeline.py::_telemetry_provider` stellt das her, statt es nur zu filtern). `retry_count` zaehlt endlich die Tenacity-Wiederholungen der vier Direktprovider (`before_sleep=note_retry_attempt`): drei bezahlte Requests = eine Zeile mit `retry_count=2`.
- ⚠ **Nicht verwechseln:** `app/intelligence/` (ADR 0015) ist NICHT Teil dieser Schicht — entgegengesetzter Vertrag (fail-closed, „no silent cloud fallback", `influences_execution=false`), synchrones Provider-Interface, Status QUARANTINE. **Aber:** seit D-CORE-007 schreiben ihre BEZAHLTEN Ausgaenge (`_ok`/`_fail`, nur `ClaudeProvider` → `provider="anthropic"`, `use_case="research"`) eine Zeile in `llm_telemetry.jsonl`. Sonst waere ein taeglicher bezahlter Aufruf (`kai-llm-shadow.timer`) fuer jedes Budget und fuer `/health/ai` unsichtbar. `NoOpProvider`, `MockProvider` und `OllamaProvider` bleiben bewusst uninstrumentiert — gratis, und sie wuerden die Unbekannt-Quote verderben.

### Konfiguration / Boot (KAI CORE v1)
- `app/core/config_redaction.py` → `redacted_config_snapshot(settings)`: effektive Konfiguration ohne Secrets (Fingerprint `(set:xxxxxxxx)`/`(empty)`, URL-userinfo maskiert) + `explicit`-Liste (env-gesetzte Felder je Sektion) — Endpoint `GET /health/config`; bewusst NICHT in `settings.py` (God-File-Ratchet)
- `app/security/secrets.py::validate_secrets` → fail-closed nur bei `APP_ENV=production` (auf dem Pi bis zur Operator-Umstellung NICHT gesetzt → warn-only); Production verlangt explizites `DB_URL` (`model_fields_set`)
- `deploy/systemd/*.service` → `EnvironmentFile=` ohne Minus in allen 59 Units (fehlende `.env` = Startverweigerung); Apply auf dem Pi operator-privilegiert (`scripts/pi_apply_systemd_units.sh`)
- Kern-Doku: `docs/KAI_CORE_V1.md` (8 Schichten → Komponente, KEEP/FIX/MERGE/DELETE/QUARANTINE/DEFER, Deploy-Pfad, Operator-Befehle)

### Digest / CLI / API / Audit / Regime
- `scripts/operator_digest.py` (tägl. Telegram-Digest; `collect_*`→`compose_digest_message`; inkl. `collect_edge_discovery` = jüngster `artifacts/research/edge_search_*.json` als 🔎-Sektion) · `app/cli/commands/daily_strategy.py` → `daily_strategy_app`
- `app/core/runtime_identity.py` → Runtime-Identität (STAB-02): Commit im Prozess vs. Checkout, Drift, Lock-Hash; `/health`-Felder `runtime_commit/checkout_commit/drift_commits`, Artefakt `artifacts/runtime/runtime_identity.json`, Health-Check `_check_runtime_identity`, Deploy-Token `RUNTIME_*` in `scripts/lib/pi_deploy_verdict.sh`
- `app/cli/main.py` → Typer-Entry, Console-Scripts `kai` (kanonisch, D-236) + `trading-bot` (veralteter Alias) (Gruppen: ingest / pipeline / signals / alerts / analyze / trading / audit / learning)
- `app/api/routers/` → `dashboard`, `signals`, `premium_signals`, `operator`, `alerts`, `health`, `tradingview`, `kyt`, `agents` …
- `app/api/routers/metrics.py` → `/metrics` Prometheus-Textformat für Event-Loop-Lag, Route-Latenz und Prozess-Uptime (STAB-03; Messung ohne Alarme)
- `app/api/main.py` → `create_app`; **Middleware-Reihenfolge ist heikel**: `GZipMiddleware` MUSS als erstes registriert (= innerste Schicht) bleiben, sonst streamen die `BaseHTTPMiddleware`-Schichten (RequestGovernance/SecurityHeaders) die Antwort und `minimum_size` verpufft
- `app/observability/operator_board_live.py` → `build_live_board`/`open_preregs`/`curated_is_stale` (rein, kein I/O) — LIVE-Hälfte von `/dashboard/api/operator-board`: `prereg_ledger.jsonl` minus `prereg_verdicts.jsonl`, ergänzt um Reife + terminale Resolution aus `research/prereg_maturity.py`. Der Reife-Reader vertraut Resolutionen ausschließlich nach Vollverifikation von `artifacts/truth/attestation_ledger.jsonl`; `RESOLVED` verschwindet aus offen, kaputte/widersprüchliche Evidenz bleibt sichtbar als HOLD. ⚠ `INSUFFICIENT_N` ist NICHT terminal; Stale-Alarm nur bei OFFENEN kuratierten Punkten, Chronik (`status: done`) veraltet nie
- `app/research/prereg_maturity.py` → `MATURITY_SPECS` (hartcodierte Spec-Registry, ein Eintrag je offener Prä-Reg) + `compute_maturity`. `kind` wählt den Zähler: `documents` (DB-Proxy, Obergrenze — darf NIE `JUDGEABLE` melden) · `tech_precision`/`exec_translation`/`hit_to_win` (exakter Evaluator aus `quote_evals.py`, identische Populations-Definition wie das spätere Verdikt) · `deadline` (reines Fenster). ⚠ **`window_end_utc` ist auf n-basierten Specs optional und additiv**: läuft die Frist ab, BEVOR `n_target` erreicht ist, wird der Claim `EVAL_CHECK_DUE` mit `state_source="window_timeout"` und `timed_out=true` — zu lesen als INCONCLUSIVE_BY_TIMEOUT, **kein Sachverdikt**. Ein erreichtes `n_target` bleibt `JUDGEABLE` (die Frist bremst keine Evidenz). Ohne Frist bleibt das Verhalten unverändert; H1 (`fd6f5f78`) trägt bewusst keine. Grund für die Bauart: H2 stand dauerhaft bei 14/50 und konnte weder PASS noch FAIL werden
- Dashboard-TTL-Caches: Quality-Single-Flight in `app/api/routers/dashboard_quality_cache.py` (Refresh injiziert aus `routers/dashboard.py`); übrige Dashboard-Caches in `routers/dashboard.py` (`_priority_gate_`/`_n_overview_`/`_operator_board_cache`). Pflicht, weil die Endpoints Multi-MB-JSONL full-scannen; neue Cache-Dicts IMMER auch in `tests/unit/test_api_dashboard.py::_patch_artifacts` zurücksetzen, sonst antwortet ein Test mit den Artefakten eines anderen
- `app/audit/kai_audit_service.py` → `KaiAuditService` (Tamper-evident Hash-Chain)
- `app/regime/classifier.py` → `classify_raw` · `app/regime/models.py` → `RegimeClass` (TREND_UP/DOWN/BREAKOUT/CHOP/UNKNOWN)

### Lightning-Credentials + Empfangspfad (Capability-Split W0/PR-A, Altpfad-Rueckbau ADR 0018 §12)
- `app/core/lightning_settings.py` → `LightningSettings.macaroon_credentials(scope)` mit `read|invoice|payment|onchain|channel`; **kein Write-Fallback** aufs Read-Credential (fehlender Scope → `LightningUnavailableError`). `validate_lightning_boot` bricht seit PR-C den START ab, wenn eine EINGESCHALTETE Capability kein Credential hat (C-1: `l402_enabled`/`receive_enabled`→invoice, `pay_enabled`→payment) — statt still 503 pro anonymer Anfrage
- `app/lightning/adapter.py::_build_client(cfg, credential_scope="read")` → JEDER Konsument fordert seinen eigenen Scope an. Nach dem Altpfad-Rueckbau (ADR 0018 §12) sind es noch drei: `receive_gate.create_invoice`+`earnings_booking`→`invoice`, `payments/rails/lightning.py::pay`→`payment`, alle Lesepfade→`read`; `onchain`/`channel` haben keinen Aufrufer mehr (ADR §1 DEFERRED), die Credentials bleiben deklariert. Der `payment`-Client ist zusätzlich strukturell an `APP_LN_PAY_ENABLED=true` gebunden, sodass auch Rohhelfer/Preflight ihn disarmed nicht materialisieren können (Tests: `test_lightning.py::test_every_money_path_requests_its_own_capability_scope`, `test_payment_client_requires_the_value_layer_kill_switch`)
- `app/lightning/receive_gate.py::create_invoice` → **der einzige Einnahmepfad**: der L402-Mint aus `truth_oracle`. Gegatet über `receive_enabled` (NICHT `pay_enabled`), dry-run-Default, scope-minimales `invoice`-Credential. Die Memo-Semantik `kai-oracle:{scope}` ist Vertrag — `earnings_ledger` ordnet daran die Quelle zu. Bewusst NICHT auf `PaymentService.create_invoice` migriert: das journalliert die Forderung und schöbe den anonymen Mint hinter den exklusiven Geldjournal-Lock (BL-2). Der Sendepfad `pay_invoice` liegt seit ADR 0018 §12 ausschließlich in `app/payments/rails/lightning.py`
- `app/lightning/client.py::list_payments` → read-only LND-`GET /v1/payments` mit `include_incomplete`, `omit_hops=true`, redigierten `LndPayment`-Zeilen (kein Preimage/BOLT11/Route/Hop) und richtungsabhängig validierter Pagination als `LndPaymentPage`; noch ohne Konsument (T6a-Voraussetzung für den separaten Reconciliation-Timer)
- `scripts/ln_reconcile.py` + `kai-ln-reconcile.timer` → der **einzige** Reconciler (ADR 0018 §8). Seit §12 PR 2 fährt er nur noch `reconcile_payments()` über `artifacts/payments/payment_journal.jsonl`; die v2-Hälfte (`lightning/reconciliation.py`, PR-D Crash-Gap-Abgleich) ist gelöscht. Name und Kadenz bleiben — ein umbenannter Timer wäre ein Fenster ohne Reconciler. Er **sendet nie** (`lookup`/`list_payments`/`invoice_status`, Read-Credential); Timer install-only, nicht Boot-/Server-gekoppelt. Er ist zugleich die **Lebend-Wache des Geldpfads**: `check_payment_reconciliation` meldet ein `last_run_utc` älter als 45 min in `artifacts/payments/reconcile_state.json` als P0. Runbooks: `docs/runbooks/payment_fabric.md` (lebend), `docs/runbooks/ln_reconciliation.md` (Archiv)
- **Ein Geldjournal, ein Archiv (ADR 0018 §12, PR 2 vollzogen 2026-09-07):** `artifacts/payments/payment_journal.jsonl` trägt jede Wertbewegung. Das alte v2-Journal `artifacts/ln_ops_ledger_v2.jsonl` hat **kein Modul mehr** — `app/lightning/ops_ledger.py`, `lightning/reconciliation.py` und `payments/reconcile_dual.py` sind gelöscht, keine Zeile Produktionscode liest oder schreibt die Datei noch. Sie bleibt am Gerät und im Backup-Vertrag (Beweis, nicht Code). Nicht gelöscht: `dual_journal_conflict` bleibt in `AUDIT_EVENT_TYPES` und im `JournalIndex` — das Geld-Journal wird nie rotiert und `JournalEvent` validiert `event_type` auch beim LESEN; ein Vokabular schrumpft nicht mit seinem Schreiber. `app/lightning/receive_ledger.py` auditiert Invoice-Mints in eigener Datei, ohne Lock, best-effort — der `/oracle`-Einnahmepfad darf nie an einem Journal scheitern (BL-2/M-9) — und trägt seit §12 auch die Redaktionsgrenze `redact_ln_op_record`
- `app/payments/policy.py::RULE_CHAIN` = **die einzige Regelkette** (zwölf Regeln, erste DENY gewinnt). `reserve_floor` steht darin VOR `amount_limits` und ersetzt den Kapital-Backstop des gelöschten `lightning/policy.py`: Quelle ist `APP_PAYMENT_RESERVE_FLOOR_SAT` (Default 0 = aus), und eine FEHLENDE Liquiditätsmessung ist bei bewaffnetem Boden ein DENY. `ln_control.ACTIONS` kennt nur noch `pay_invoice` (delegiert) und `create_invoice` (Empfangs-Gate)
- `app/lightning/golive_preflight.py` + `scripts/ln_golive_preflight.py` → Bake-Gate: prüft Read- und Invoice-Credential getrennt, probt `pay_invoice` gegen **beide** Empfangs-Credentials; armed zusätzlich gegen `APP_LN_PAYMENT_MACAROON_*`. Matrix: `docs/lightning_macaroon_matrix.md`
- Der Armed-Send-Probe ist dreiwertig: Permission-Deny=`True` (nicht sendefähig), explizite Invalid-Invoice-Antwort nach Permission=`False` (sendefähig), Transport/TLS/Timeout/unklar=`None` (unbewiesen ⇒ NO-GO). Ein Transportfehler darf nie als sendefähiges Credential gelten (B-6)
- `app/security/hotp_auth.py` + `scripts/hotp_bootstrap.py` → HOTP-Read→Verify→Append unter strict Lock + `fsync`; fehlendes/leeres/korruptes Journal ist **deny**, Erst-Inbetriebnahme nur explizit mit `--next-counter` (bestehende Journale werden nie überschrieben)
- `app/security/auth.py::_LN_LOCAL_BYPASS_READS` → die Regel ist INVERTIERT: jeder `/dashboard/api/ln/*`-Pfad braucht starke Auth, außer er steht ausdrücklich in dieser Read-Allowlist. `/dashboard/api/ln/ops` ist mit ADR 0018 §12 ersatzlos entfallen (es las das Archivjournal); die Audit-Sicht auf den lebenden Geldpfad ist `GET /payments/audit` und verlangt bewusst eine `intent_id`

### Payment Control Plane (`app/payments/`, ADR 0018)
- **Der Serialisierungspunkt** ist `PaymentService.create_intent`: Idempotenz-Konsum, Cap-Lesung aus dem Index, Policy-Verdikt und beide Records laufen unter EINEM `journal.transaction()`. Decode und Node-Health passieren VORHER und sind ausdrücklich Vorschau — ein Node-Aufruf unter dem Lock hielte jeden anderen Schreiber für die Dauer eines Netzaufrufs an
- **Der einzige `rail.pay()`-Aufruf im ganzen Repo** steht in `PaymentService.execute`. `ln_control` delegiert seit ADR §12 dorthin (`ln_control_delegate.py`); seit dem Rückbau ist der zweite Sendeweg nicht mehr abgezweigt, sondern GELÖSCHT — `value_layer.py` existiert nicht mehr, ein AST-Test in `test_ln_receive_gate_split.py` verbietet jeden schreibenden Node-Aufruf in `app/lightning` außer `add_invoice`
- `artifacts/payments/payment_journal.jsonl` — **das eine Geld-Journal**: append-only, `prev_hash`/`record_hash`, fsync, `0600`, **nie rotiert**, portalocker-Interprozess-Lock auch für LESENDE Zugriffe. Torn Tail = Deny (nicht „auf der letzten lesbaren Zeile weiterschreiben“ — das forkt still). Pfad NUR über `PaymentSettings.journal_path`, absolut zur Repo-Wurzel
- `app/payments/status.py::transition` — **die einzige Vergabestelle** für `PaymentStatus`. Nach einem Submit ist jede Aussage über den Verbleib des Geldes beweispflichtig (`rail_evidence`); ohne Beleg bleibt nur `RECONCILIATION_REQUIRED`. `FAILED_FINAL` = *bewiesen nichts bewegt*, `RECONCILIATION_REQUIRED` = *unbekannt*
- `app/payments/reconcile.py` + `reconcile_passes.py` → drei Durchgänge (vorwärts `lookup`, rückwärts `list_payments` → `orphan_settlement`, Forderungen `invoice_status` → `receivable_settled`). Läuft im bestehenden `scripts/ln_reconcile.py::reconcile_payments`; Zustand in `artifacts/payments/reconcile_state.json`
- `app/payments/receivables.py::settle_receivable` → **der einzige Schreiber von `receivable_settled` im ganzen Repo** (D-CORE-006, AST-Test `tests/unit/pay/test_pay_never_writes_money.py`). Er ist ein Durchgang des Receivables-Passes für GENAU EINEN `ref_hash`: erst `refresh_tail()` (der Timer schreibt in dieselbe Datei), dann `settlement_of` (schon gebucht ⇒ kein Rail-Aufruf, kein Append), dann `invoice_status`. Der Reconcile-Timer fährt ihn in einer Schleife, KAI PAY für eine einzelne Forderung — zwei Takte, ein Schreiber, keine Doppelbuchung. Ein Rail-Fehler liefert `settled=None` (**keine Aussage**), nie `False`
- `app/payments/journal_chain.py::attest_payment_journal_tip` → bindet den verifizierten Journal-Tip als `payment_journal_tip` in die Truth-Kette, deren Tip per OTS on-chain geht (`scripts/truth_anchor_run.py`, best-effort/BL-1). Ersetzt `ops_ledger.attest_ln_ops_tip`; ohne ihn wäre nach dem Rückbau **keine Geldbewegung mehr on-chain verankert**. Eine gebrochene Kette wird VERWEIGERT, nicht attestiert
- `app/payments/health.py` → `GET /health/payment` (auth-gated wie `/health/ai`). Kein Grün ohne Beweis: gebrochene Kette ODER `reconciliation=attention` ⇒ `degraded`. `/health/config` zeigt die `payments`-Sektion über `config_redaction(extra_sections=…)` — `PaymentSettings` hängt bewusst NICHT in `AppSettings`

### KAI PAY (`app/pay/`, D-CORE-006, [docs/KAI_PAY_V0_1.md](KAI_PAY_V0_1.md))
- Produktschicht **über** dem versiegelten Kern: Zahlung anfordern → per Lightning bezahlen → Eingang automatisch bestätigen. Richtung `pay → payments`, nie umgekehrt (`test_payment_dependency_direction.py`). Default **AUS** (`APP_PAY_ENABLED`), `/pay/*` antwortet dann 404 `kai pay disabled`
- **Geldwahrheit ausschließlich aus dem Kern:** `settle_receivable` (buchen), `settlement_of` (Betrag/Zeit/`journal_seq`/`record_hash`), `expiry_of` (Frist aus dem `intent_created`-Record). `app/pay` importiert `PaymentJournal` nicht und ruft nirgends `journal.append`/`transaction` — per AST erzwungen
- `artifacts/pay/requests.jsonl` — **Cache, keine Wahrheit**: `payment_id ↔ ref_hash`, Beschreibung, Referenz, Webhook-Ziel, BOLT11 (der QR nach einem Reload) und ein Zeiger auf den Journal-Record. Ein Prozess, kein Lock, keine Kette, `O_APPEND`+fsync — dieselbe Abwägung wie `lightning/receive_ledger.py`. Bewacht von `_check_pay_requests` (Form, nicht Kadenz)
- `app/pay/status.py` — vier Worte nach außen: `WAITING | SETTLED | EXPIRED | FAILED`. `SETTLED` ist final; `EXPIRED` verlangt eine **Rail-Aussage** (ohne sie bleibt es `WAITING`, fail-soft mit `last_error`); `FAILED` ist heute unerreichbar, weil kein Rail eine endgültige Absage liefert
- `app/pay/poller.py` — In-Prozess-Takt im `kai-server`-Lifespan (nur bei `enabled`). ZWEI Zeitgrenzen: 30 s je Nachfrage und 300 s je Runde — die zweite ist keine Summe der ersten, sondern die Wache gegen einen Poller, der aussieht, als liefe er. Steht `kai-server`, bestätigt weiterhin der 15-Minuten-Reconciler
- Webhook: `X-KAI-Pay-Signature: sha256=HMAC(secret, body)` über **die gesendeten Bytes**. Ohne `APP_PAY_WEBHOOK_SECRET` **kein Versand** (Event `webhook_failed`, Grund `no_secret`) — ein unsignierter Callback ist eine Nachricht, die jeder fälschen kann, der die URL kennt
- **Startguard:** ist `enabled` und `APP_PAY_PURPOSE` fehlt in `APP_PAYMENT_PURPOSES_ALLOWED`, startet `kai-server` nicht. Sonst scheiterte jede Forderung erst NACH ihrem Journal-Record

**Zählfallen (hier stolpert man):**
- `JournalIndex.open_intents()` ≠ `all_intents()`: die Rückwärts-Richtung fragt „gehört dieser Send zu UNS?“ — nur `open_intents` zu benutzen erklärte jede abgeschlossene Zahlung beim nächsten Lauf zur Waise
- `RailPaymentList.window_enforced` ist beim `LightningRail` **`False`**: `client.list_payments` reicht kein `creation_date` durch, also kann `since` nicht eingehalten werden. Folge: bei der Inbetriebnahme wird die Alt-Historie EINMAL als Waise gemeldet, danach nie wieder (`JournalIndex.orphan_keys`)
- Ein Reconcile-Lauf ohne Statuswechsel schreibt **keinen** Record. Ungeklärte Vorgänge stehen trotzdem im Report (`unresolved`) — sonst wäre ein Journal voller offener Sends von einem sauberen nicht zu unterscheiden
- Tagesgrenze des Caps ist **UTC** (`JournalIndex._day_key`), nicht lokal — auf einem Pi in CEST spränge der Cap sonst zweimal im Jahr
- `Money.minor_units >= 0`, aber `PaymentIntent.amount_requested > 0`: eine Routing-Gebühr von 0 ist real, ein Zahlbetrag von 0 nicht
- Settlement-Latenz misst `submitted → settled`, nicht die Node-Zeit: wie lange die Zahlung UNKLAR war. Bei n=0 ist sie `None`, nie `0.0`
- Der Uhr-Sprung-Guard setzt Ablauf-Übergänge auch dann aus, wenn es KEINE vergleichbare Basislinie gibt (erster Lauf, Neustart — die monotone Uhr ist boot-relativ). `EXPIRED` ist terminal

### KAI PAY v0.1 — Mini-Oberfläche (`web/`, Vertrag `/pay/*`)
- `web/src/pages/Pay.tsx` → **die eine Seite** (Route `#pay`, Sidebar-Gruppe Souveränität): Formular (Betrag in sat, Beschreibung, Referenz) → `POST /pay/requests` → QR aus `lightning_uri` + kopierbares BOLT11 + Ablauf-Countdown → Status-Polling alle 3 s bis `SETTLED|EXPIRED|FAILED` → Receipt (`GET /pay/requests/{id}/receipt`) + Liste der letzten 10. `GET /pay/health` 404 (`kai pay disabled`) ⇒ Hinweisseite statt Formular; API-Fehler werden immer angezeigt, nie stiller Fehlzustand
- `web/src/lib/api.ts::createPayRequest|fetchPayRequest|fetchPayRequests|fetchPayReceipt|fetchPayHealth` → die Fetcher (Same-Origin, kein Bearer — CF-Access-Header wie alle Dashboard-Endpunkte). `web/src/lib/pay.ts` = reine Logik (Status→Ton, Terminalität, Eingabe-Validierung, QR-Payload in GROSSBUCHSTABEN, Countdown), `web/src/lib/usePayRequestPolling.ts` = der Poller, der bei Endzustand aufhört. QR-Rendering: `web/src/components/panels/PayQr.tsx` über npm `qrcode` (einzige neue Abhängigkeit, landet im lazy `Pay`-Chunk)
- ⚠ Vertragslücke: `GET /pay/requests/{id}` liefert **kein** `bolt11`/`lightning_uri` — die Seite hält Invoices nur für in dieser Sitzung erzeugte Requests im Speicher; ein aus der Liste geladener Request zeigt Status/Betrag, aber keinen QR

## Kern-Env-Flags (Definition; LIVE-Werte = Pi-`.env`, NICHT hier)
- `EXECUTION_ENTRY_MODE` → `settings.execution.entry_mode` (`EntryMode`) — Master-Entry-Kill-Switch
- `EXECUTION_PAPER_MIN_PRIORITY` — Paper-Fill-Prioritätsschwelle
- `SOURCE_CRYPTO_RELEVANCE_GATE_MODE` (off/shadow/enforce) — Pre-Analyse-Relevanz-Gate
- `APP_HYPE_EVIDENCE_ENABLED` (env_prefix `APP_HYPE_EVIDENCE_`, `evidence_settings.HypeEvidenceSettings`) — HYPE-Evidence an/aus
- `PREMIUM_FASTLANE_ENABLED` (`PremiumFastlaneSettings`) — Fastlane (dauerhaft false)
- `RISK_MAX_OPEN_POSITIONS` — Positionslimit (nur Pi-`.env`)

## Kern-Artefakte (`artifacts/*.jsonl`)
- `paper_execution_audit.jsonl` — Paper-Fills/Closes/PnL (Replay-SSOT)
- `shadow_candidate_resolved.jsonl` — resolved Generator-Kandidaten + `fwd_*_bps`
- `shadow_real_feed_funnel.jsonl` — Funnel seen→eligible→injected→candidate
- `blocked_outcomes.jsonl` — geblockte Alerts + ~28h-Outcome (asset/dir/move im `note`)
- `blocked_alerts.jsonl` — geblockte Alerts (reason; KEIN Symbol/Dir)
- `payments/payment_journal.jsonl` — **jede Wertbewegung** (ADR 0018 §5); nie rotiert, `0600`, hash-verkettet. Zusammen mit `ln_ops_ledger_v2.jsonl` und `ln_hotp_journal.jsonl` in `kai_backup_artifacts.sh::DEFAULT_SOURCES`; `MONEY_SOURCES` bricht ab, wenn eines davon aus dem letzten Archiv-Manifest verschwindet. ⚠ `ln_ops_ledger_v2.jsonl` ist seit ADR 0018 §12 PR 2 ein **Archiv ohne Code** — weder Schreiber noch Leser; die Datei bleibt liegen und bleibt im Backup-Vertrag; sie zu „räumen“ ließe den `VANISHED_MONEY`-Guard zuschlagen
- `alert_outcomes.jsonl` — resolved directional alerts (hit/miss)
- `app/intelligence/` — Local Intelligence Layer (ADR 0015): TaskRouter/Provider/ContextBuilder/Audit; Flags `KAI_LLM_*` (default-off); Trail `artifacts/intelligence_audit.jsonl`; CLI `intelligence {daily-summary,anomaly-explain,doc-qa}`
- `funding_evidence_shadow.jsonl` / `oi_evidence_shadow.jsonl` / `hype_evidence_shadow.jsonl` — V5-Evidence (shadow)
- `bridge_pending_orders.jsonl` — TV-Bridge pending/promoted
- `trading_loop_audit.jsonl` — Cycle-Trace · `decision_journal.jsonl` — Operator-Entscheide · `risk_gate_audit.jsonl` — Gate-Decisions
- **Wachstum/Lesekosten (2026-07-30):** `app/storage/jsonl_io.py::iter_jsonl_since` = Fenster-Read für zeitsortierte append-only Streams (rückwärts in 256-KiB-Blöcken, Stop erst nach `_DISORDER_MARGIN_RECORDS=64` älteren Datensätzen). Genutzt von `build_priority_gate_summary` + `compute_loop_idle_signal`; gemessen 0,74 s → 0,03 s bei identischem Ergebnis. ⚠ Setzt LEXIKOGRAFISCH vergleichbare ISO-Stempel voraus — nur bei Call-Sites einsetzen, die ohnehin so verglichen haben (`daily_briefing`/`canonical_read` parsen zu `datetime` → NICHT umgestellt). ⚠ Streams sind append-only, aber nicht streng monoton (143 Verletzungen / 88.321 Zeilen, max. 7 Zeilen Versatz) — daher die Marge.
- **Rotation:** `scripts/audit_rotate.py` (`kai-audit-rotate.timer`, täglich 04:40, LIVE) archiviert allowlistete Streams nach `artifacts/archive/`, löscht nie. Ein Stream kommt nur auf die Allowlist, wenn JEDER Konsument höchstens ein Fenster braucht. `paper_execution_audit.jsonl` (Replay-SSOT) + `blocked_outcomes.jsonl` hart ausgeschlossen; `trading_loop_audit.jsonl` bleibt trotz Grösse aussen vor (Voll-Historie-Leser: `daily_briefing`, `health_check`, `loop_idle_signal`, `canonical_read`) — dort ist der Fenster-Read der Hebel, nicht das Kürzen.

### Research Brief freshness
- `app/core/briefs.py::ResearchBriefBuilder.build` → rolling UTC window (default 24h), generated_at, source timestamp bounds, count, current/no_current_data; missing/future/old source times excluded. SQLite-naive publication timestamps explicitly interpreted as UTC.
- `app/api/routers/research.py` → `/research/brief?watchlist=…&window_hours=24`. Der Kandidaten-Scan ist FENSTERGEBUNDEN (`repo.list(published_after=window_start, limit=limit*5)`) — sonst filtert die Watchlist auf einem Stapel, der ueberwiegend ausserhalb des berichteten Fensters liegt, und der Bericht ist leer trotz frischer Daten. ⚠ **Drei** Aufrufer, alle drei muessen dasselbe Fenster benutzen: `app/api/routers/research.py`, `app/cli/commands/research_core.py`, `app/agents/tools/canonical_read.py::get_research_brief` (der Agenten-Pfad wurde beim ersten Umbau uebersehen). Trial: `KAI_RESEARCH_TRIAL.md`; cost audit: `KAI_COST_SURFACE.md`.
