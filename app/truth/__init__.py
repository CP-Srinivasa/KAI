"""Verifiable truth primitives (ADR 0013, Tier-1 frontier).

Deterministic, recomputable attestations over signals + provenance so third parties
can verify a published claim was not altered — verifiable truth instead of trust.
"""
