"""Compliance nachweis-hygiene (ADR 0013).

Records provenance/ownership evidence and derives a consolidated SoF/TFR/tax
export. Purpose is to PROVE provenance (audit-to-prove), never to obscure it — the
export deliberately surfaces gaps where evidence is missing.
"""
